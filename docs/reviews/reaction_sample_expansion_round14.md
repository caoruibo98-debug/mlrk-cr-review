# Round14 Reaction Sample Expansion Review

Date: 2026-06-04

## Direct Answer

Ray's diagnosis is mostly right, but the production blocker has two layers:

1. **Reaction-family coverage is incomplete.** Rhea supports some missing families, but gut-specific reactions such as urolithin, equol, isoxanthohumol, and flavanol ring-fission remain sparse in curated reaction databases.
2. **Generator recall is still the sharper blocker.** Several known gold positives already exist, but clean candidate generation does not retain the true product. Retraining cannot fix a product that never reaches the ranker.

The current high scores are not production evidence because `clean2_metrics.csv` evaluates only small panels (`A/LTR_full:n=10, B/LTR_full:n=10, C/LTR_full:n=10, D/LTR_full:n=10`).

## What Was Run

- PubMed Round14 search: `14` query rows, `70` returned PMID slots, `63` unique PMID records parsed.
- Rhea Round14 search: `15` query terms, `83` returned reaction rows.
- GitHub standards checked: ECREACT/RXN biocatalysis, RetroPathRL/RetroRules, gapseq, and EnzymeMap literature.

## Main Production Diagnosis

Blocker counts: `{'generator_recall_blocker_plus_family_coverage': 4, 'source_backed_sample_coverage_gap': 3, 'evidence_extraction_gap': 1, 'source_sparsity_gap': 1}`

- `polyphenol_ring_fission`: `evidence_extraction_gap`; There is external evidence, but it is not yet exact/source-mapped enough for labels.
- `urolithin_dehydroxylation`: `generator_recall_blocker_plus_family_coverage`; Existing gold edge is missing from clean candidates; fix generator before retraining or duplicate imports.
- `hydroxycinnamate_reduction_and_hydrolysis`: `source_backed_sample_coverage_gap`; Rhea provides source-backed reactions that can expand positives after mapping and generator checks.
- `isoflavone_reductive_and_glycoside_metabolism`: `source_backed_sample_coverage_gap`; Rhea provides source-backed reactions that can expand positives after mapping and generator checks.
- `lignan_redox_and_deglycosylation`: `generator_recall_blocker_plus_family_coverage`; Existing gold edge is missing from clean candidates; fix generator before retraining or duplicate imports.
- `prenylflavonoid_o_demethylation`: `generator_recall_blocker_plus_family_coverage`; Existing gold edge is missing from clean candidates; fix generator before retraining or duplicate imports.
- `glycoside_and_hmo_hydrolysis`: `generator_recall_blocker_plus_family_coverage`; Existing gold edge is missing from clean candidates; fix generator before retraining or duplicate imports.
- `bile_acid_deconjugation_and_lipid_context`: `source_backed_sample_coverage_gap`; Rhea provides source-backed reactions that can expand positives after mapping and generator checks.
- `amino_acid_catabolism_and_redox`: `source_sparsity_gap`; Public exact-reaction evidence is sparse; do not invent reactions or treat no-hit as negative.

## Safe Positive Expansion Candidates

Round14 found `4` Rhea source-backed reaction candidates and `3` PubMed rows that are already known/supporting context. **None are allowed into training immediately.**

- `RHEA:16309`: taurochenodeoxycholate + H2O = chenodeoxycholate + taurine
- `RHEA:19353`: glycocholate + H2O = cholate + glycine
- `RHEA:20689`: chlorogenate + H2O = L-quinate + (E)-caffeate + H(+)
- `RHEA:69683`: daidzein 7-O-beta-D-glucoside + H2O = daidzein + beta-D-glucose + H(+)
- `PMID:12736449`: existing_gold_exact_or_support: pinoresinol -> lariciresinol
- `PMID:31138818`: existing_gold_exact_or_support: 2-FL -> L-fucose
- `PMID:41797252`: supporting_context: urolithin dehydroxylation

Why not train yet:

- structures must be mapped to local SMILES/InChIKey;
- duplicate existing positives must be excluded;
- food-gut relevance and source license must be recorded;
- the deployment-faithful generator must enumerate the true product;
- split leakage must be checked.

## Negative Sample Logic

Round14 found `17` PubMed hits in equol/non-producer/no-conversion searches, but these are not negative labels until exact assay conditions are extracted.

- `assay_negative`: allowed_now=False; Paper states exact substrate, exact organism/strain/community, assay condition/time, detection method, and explicit no-product/no-conversion outcome.
- `conditional_genome_or_model_absence`: allowed_now=False; Versioned genome/model lacks required enzyme/pathway under defined strain and substrate scope; recorded as conditional, not global.
- `hard_decoy_for_ranking`: allowed_now=True; Generated by allowed rules, not known positive, structurally plausible, split-safe, and labeled as ranking decoy not biological false.
- `pubmed_or_rhea_no_hit`: allowed_now=False; None: no-hit is only a search gap.

Hard decoys are the only Round14 route that can be used now, and only as ranking decoys, not biological negatives.

## Files Created

- `data/curation/round14_external_model_standards.csv`
- `data/curation/round14_pubmed_query_audit.csv`
- `data/curation/round14_pubmed_candidate_triage.csv`
- `data/curation/round14_rhea_reaction_triage.csv`
- `data/curation/round14_reaction_type_blocker_diagnosis.csv`
- `data/curation/round14_positive_sample_candidates.csv`
- `data/curation/round14_negative_sample_strategy.csv`
- `data/curation/round14_collect_verify_modify_pipeline.csv`

Raw evidence:

- `data/curation/ncbi_round14_raw/*.json`
- `data/curation/rhea_round14_raw/*.json`
- `data/curation/ncbi_round14_raw/*.xml` is intentionally ignored by git.

## Round15 Direction

1. Map structures for Rhea positive candidates, starting with chlorogenate hydrolysis, daidzein glycoside hydrolysis, and bile acid deconjugation.
2. For known gold generator misses, continue generator overlay repair before importing duplicate labels.
3. For negative samples, extract explicit no-conversion assay rows only; otherwise keep rows as unlabeled or hard decoys.
4. After mapping, run generator recall before any retraining.

## Claim Boundary

Allowed after Round14:

> We have a source-backed queue of candidate positive reactions, a strict negative-sample policy, and a diagnosis separating reaction coverage gaps from generator recall failures.

Forbidden:

> Round14 has expanded the training set or proved production readiness.
